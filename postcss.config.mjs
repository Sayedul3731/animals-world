// postcss.config.mjs

import tailwindcss from "tailwindcss";

const config = {
  plugins: [
    tailwindcss,
    // Add autoprefixer or any other PostCSS plugins here if needed
  ],
};

export default config;
